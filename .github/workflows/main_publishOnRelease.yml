name: Publish PowerShell Module

on:
  release:
    types: [published]

jobs:
  publish-to-gallery:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and publish
        env:
          NUGET_KEY: ${{ nuget.API_KEY }}
        shell: pwsh
        run: |
          Invoke-Build
          Publish-Module -Path /build/SeededGuid -NuGetApiKey $env:NUGET_KEY -Verbose
